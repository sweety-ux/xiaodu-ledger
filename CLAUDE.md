# 黑马记账 APP — 产品文档与项目规则

## 一、项目概述
- 产品名称：黑马记账
- 产品定位：一款运行在 Windows 电脑上的个人记账工具，记录每一笔花销（人民币）
- 目标用户：非技术背景的个人用户
- 核心原则：简单、好用、数据不丢

## 二、产品功能（第一版·标准版）
1. **记一笔**：金额（元）、日期、二级分类（先选一级大类，再选二级小类）、备注（选填）
2. **账单列表**：按日期分组展示所有记录，每条可**编辑**、可**删除**
3. **月度统计**：选择月份 → 本月总支出、各一级大类汇总金额、各二级小类汇总金额
4. **图表统计**：分类占比饼图（按一级大类）、月度每日支出趋势柱状图
5. **数据安全**：
   - 数据保存在浏览器本地（localStorage）
   - 每日自动保存快照（保留最近 30 天，可随时一键恢复）
   - 支持手动**导出备份**（JSON 文件）和**导入恢复**，换电脑也能带走数据

## 三、后续可选功能（暂不做，用户要求时再评估）
- 预算设置与超支提醒
- 搜索 / 筛选
- 导出 Excel
- 收入记账
- 分类管理（自定义分类）

## 四、支出分类体系（2 级，由开发者设计）
| 一级大类 | 二级小类 |
|---|---|
| 餐饮美食 | 早餐、午餐、晚餐、外卖、零食饮料、水果、聚餐请客 |
| 交通出行 | 公交地铁、打车、共享单车、加油、停车、火车飞机、汽车保养 |
| 购物消费 | 服装鞋帽、日用品、数码电器、美妆护肤、家居用品、网购 |
| 居住物业 | 房租、房贷、水电燃气、物业费、宽带话费、房屋维修 |
| 娱乐休闲 | 电影演出、游戏充值、旅游出行、运动健身、阅读 |
| 医疗健康 | 门诊挂号、药品、体检、保健养生 |
| 学习教育 | 书籍文具、培训课程、考试报名 |
| 人情往来 | 红包礼金、随份子、礼物 |
| 其他 | 其他支出 |

## 五、技术选型（已确定：网页版应用）
- **形态**：纯 HTML + CSS + JavaScript 网页应用，双击 `index.html` 用浏览器（Edge/Chrome）打开即可使用，无需安装任何软件
- **文件结构**：
  - `index.html` — 页面结构与界面
  - `style.css` — 样式（手机 APP 风格布局，界面美观）
  - `app.js` — 记账、账单、统计的全部逻辑
  - `data.js` — 分类数据表
- **数据存储**：浏览器 localStorage（无需网络、无需服务器）
- **图表**：用 ECharts 图表库（首次联网加载后浏览器自动缓存；离线时自动退化为简洁条形图）
- **未来升级**：用户想要独立桌面窗口时，可无痛升级为 Electron 桌面软件（代码复用）

## 六、项目协作规则（重要，整个项目期间必须遵守）
1. **用户不懂编程**：所有技术解释必须用通俗易懂的语言（不堆术语，必要时用类比）
2. **重大技术决定必须先问用户**：凡是涉及技术选型、方案取舍、数据方案、新功能实现方式等决定，必须列出 2–4 个备选方案，逐条说明每个方案的优点、缺点、适用场景和推荐项，用提问工具（AskUserQuestion）让用户选择，**用户选定后才执行**
3. **不擅自添加功能**：用户没要求的功能不加；想加必须先说明并获得同意
4. **阶段汇报**：每完成一个阶段，用通俗语言向用户汇报「做了什么、怎么用、下一步做什么」
5. **运行方式由用户自己掌握**：交付时把「怎么打开、怎么用」写成简单步骤
